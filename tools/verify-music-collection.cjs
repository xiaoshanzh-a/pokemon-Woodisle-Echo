const {chromium}=require('C:/Users/Sheng/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(()=>{const Original=Audio;window.players=[];window.Audio=function(...args){const a=new Original(...args);players.push(a);return a}});await p.goto('http://127.0.0.1:4188');await p.waitForFunction(()=>window.echoDiagnostics?.().ready);await p.locator('#sound').click();const tracks=await p.evaluate(async()=>(await import('/music-library.js')).COLLECTION_TRACKS);
 for(const t of tracks){await p.locator('#music-select').selectOption(t.id);await p.waitForFunction(id=>players.some(a=>a.src.endsWith('/'+id+'.mp3')&&!a.paused&&a.currentTime>0&&!a.error),t.id);assert.equal(await p.evaluate(()=>players.filter(a=>!a.paused).length),1)}
 await p.locator('#sound').click();assert.equal(await p.evaluate(()=>players.filter(a=>!a.paused).length),0);assert.deepEqual(errors,[]);console.log('PASS all 12 imported tracks decode and play, selectable in menu, one stream at a time and mute');
}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
