# 九栏存档

- 旧版单一存档会自动保留到 1 号栏。
- 点击“保存旅程”打开九个栏位；可保存当前栏、复制到其他栏，或读取已有旅程。
- 保存到其他已有栏位需要二次确认。取消不会覆盖目标栏。
- “新游戏”只使用空白栏，已有栏位不可选；九栏全部占用时不会自动清除任何旅程。
- 自动保存跟随当前选中的栏位，其他栏位独立保留。
- 存档保存在当前浏览器、当前站点。背包中的导出功能可另存当前旅程备份。
- 保存失败时不切换旅程；无法读取的旧数据不会被自动保存覆盖。

验证：运行 node test-save-slots.cjs（需要本地 4188 端口服务）。测试使用独立浏览器环境，不修改玩家的实际存档。
