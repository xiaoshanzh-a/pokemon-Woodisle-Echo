# 地形衔接与植被更新 · 2026-09-18

## 修复

- 风铃公园西侧接潮汐渔港东桥：补上水岸和同宽木桥，桥面延伸到画面边缘。
- 潮岬山路西侧接星灯街东桥：同样补齐水面、桥和护岸；岸上分岔口不再穿过栏杆。
- 全部连接核查发现蒲风草原、苔影古道也有同类问题，一并修复。遗迹方向使用对应的灰色桥面。
- 四条横向支路水面设碰撞区，桥廊可通行；左右出入口及原方向关系不变。
- 潮岬山路北侧原有水域增加禁行碰撞；移走落在水上的花丛。
- 月露山庭南侧两处出口让开树冠。

## 美术调整

实际素材目录为工作区 `assets/fire_red/vegetation` 和 `assets/fire_red/terrain`。选取原 PNG 复制到运行工程 `assets/landscape-*.png`，原素材未改动。运行时复用背景色剔除方式；视觉复查后撤下显示异常的遗迹草贴片。

六条路线更换柔和渐变地表，并按区域使用金叶树、灌木、红花、草原矮松、棕榈矮丛、芦苇、岸石、岩脊、苔石、树根、树桩、菌木和符文石板。潮岬山路的大方块山体换成分层岩脊。六个主区域也追加相应植被/地形装饰，摆放时避让道路、交互点与人物视线。

装饰层没有新增任务和存档字段。保留原主线、支路观察点和入口位置。

## 验证

- `tools/verify-side-routes.cjs`：六条支路所有非野怪交互点可达，双向往返、单次奖励、保存恢复及遗迹门槛通过。
- `tools/verify-geography.cjs`：12 个往返连接、6 个主路落点、各剧情阶段唯一小遥、NPC 间距和道路故事回报通过。
- `tools/verify-coast.cjs`：六个主区域桥面连续、桥外水域不可行走。
- `tools/verify-landscape.cjs`：四条支路的桥面和水域碰撞、六路线装饰与人物视线、资源请求检查；导出 `landscape-route-0.png` 至 `landscape-route-5.png` 并逐张复查。

以上测试在独立浏览器中运行，不修改玩家浏览器存档。
